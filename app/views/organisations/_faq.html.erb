 <div class="container my-3 scrollable" id="listTips">
   <div class="accordion" id="accordionTips">
    <% @articles.each do |article| %>
    <div class="card">
      <div class="card-header" id="heading<%= article.id %>">
        <h5 class="mb-0">
          <button class="btn btn-link text-white text-left collapsed" type="button" data-toggle="collapse" data-target="#collapse<%= article.id %>" aria-expanded="true" aria-controls="collapse<%= article.id %>">
            <%= article.title %>
          </button>
        </h5>
      </div>

      <a id="article<%= article.id %>">
      <div id="collapse<%= article.id %>" class="collapse" aria-labelledby="heading<%= article.id %>" data-parent="#accordionTips">
        <div class="card-body text-left">
         <%= article.content %>
        </div>
      </div>
      </a>
    </div>

    <% end %>
  </div>
  </div>


